
/**
 * Some text in markdown syntax with optional support for resolution of
 *  backtick delimited 'code' references.
 */
function MarkdownBlock(text, codeResolver) {
  this.text = text;
  this.resolver = resolver;
}
MarkdownBlock.prototype = {
};
exports.MarkdownBlock = MarkdownBlock;


